#include <iostream>
#include <string>
#include <cmath>
#include <vector>
using namespace std;

int main() {
    long long n, m;
    cin >> n >> m;
    
    long long ans = 0;
    vector<long long> a(5,0),b(5,0);
    for(int i =0; i < n; i++){

    }

    return 0;
}